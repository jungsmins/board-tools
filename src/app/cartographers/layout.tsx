import { ReactNode } from 'react';

export const metadata = {
  title: '지도제작자들 | 보드툴즈',
};

export default function CartographersLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
